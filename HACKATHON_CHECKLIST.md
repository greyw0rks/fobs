# Hackathon checklist

**Superseded — see [HANDOFF.md](HANDOFF.md).**

This file used to track the same work as `HANDOFF.md`, and the two drifted. It
was last accurate before the Rust toolchain was installed, at which point its
"Not done — blocked" section (no `rustc`, no `cargo`, no `solana`, program "has
never been compiled") stopped being true. Keeping two lists meant the stale one
kept being read first.

The parts of it that were not duplicated — the demo path and the scope
discipline notes — have been folded into `HANDOFF.md`. That is now the single
living checklist, updated as sections are actually verified.
