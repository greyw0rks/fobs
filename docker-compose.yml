services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: fobs
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - fobs_pg:/var/lib/postgresql/data

volumes:
  fobs_pg:
