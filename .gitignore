# dependencies
node_modules/

# next
.next/
out/
next-env.d.ts
*.tsbuildinfo

# rust / anchor
target/
.anchor/
test-ledger/

# local secrets and config
.env
.env.local

# Cached devnet artifacts. usdc-mint.json is a real secret key (the test USDC
# mint authority) and the test-user wallets under here are real keypairs.
.devnet/
keys/

# generated Prisma dev schema — `pnpm db:dev` recreates it from schema.prisma
apps/web/prisma/schema.dev.prisma

# local sqlite database
apps/web/prisma/*.db
apps/web/prisma/*.db-journal

# playwright output. The specs themselves are tracked — these are the run
# artifacts (traces, screenshots, the HTML report).
test-results/
playwright-report/
blob-report/
playwright/.cache/

# os
.DS_Store
.vercel
.env*

# generated "brag" video composition artifacts — not part of the app
brag-output/
